# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenv (never commit)
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# Secrets / local env (never commit)
.env
*.pem
*.key

# Databases / local run artifacts
*.db
*.db-wal
*.db-shm
sentrik.db*
sentrik_checkpoints.db*
sentinel.db*
_live*.db*
_m*.db*

# Local object storage
sentrik_storage/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/

# --- Published-repo scope (deliberate): the public repository ships the application
# only. Tests, docs, the lab target, benchmarks, migrations and audit records live in
# the private dev tree. README states this. Remove these lines to publish them.
audit/
tests/
docs/
migrations/
scripts/
skills/
benchmarks/
vulnapp/
alembic.ini
